import 'package:flutter/material.dart';

class ProuductRatingScreen extends StatelessWidget {
  const ProuductRatingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
